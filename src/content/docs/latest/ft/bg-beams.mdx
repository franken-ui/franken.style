---
preview:
  width: "100%"
  height: "320px"
group: backgrounds
project: ft
text: Beams
title: Beams
lead: Multiple background beams that follow a path of SVG, makes a good hero section background.
meta:
  description: Multiple background beams that follow a path of SVG, makes a good hero section background.
---

import Snippet from "snippets/component.astro";
import Ec from "snippets/ec.astro";
import Installation from "components/partials/ft/installation-mdx.astro";

<Snippet project="ft" component="bg_beams.basic" />

## Installation

<Installation uid="bg-beams" />

## Props

To pass props, include them as JSON inside a `<script>` tag with the `data-fn="props"` attribute.

<Ec project="ft" path="etc.props" />

<span class="uk-label uk-label-primary">Note</span> The `data-ft-*` attribute
determines which Frankternity component is mounted and should be replaced with
the component name.

| Prop Name   | Type     | Default                                       | Description                                                    |
| ----------- | -------- | --------------------------------------------- | -------------------------------------------------------------- |
| `className` | `string` |                                               | Additional CSS classes to be applied to the outer container.   |
| `stl`       | `object` | <code class="uk-codespan">&#123;&#125;</code> | Additional inline styles to be applied to the outer container. |

<script src="https://cdn.jsdelivr.net/npm/frankenstyle@0.3.7/dist/js/ft-core.iife.js"></script>
<script src="https://cdn.jsdelivr.net/npm/frankenstyle@0.3.7/dist/js/ft/bg-beams.iife.js"></script>
