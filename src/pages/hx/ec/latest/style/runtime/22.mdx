export const partial = true;

```js
const cache = FS.getCache();
// Map(1) { 'opacity:hover:hover-false-null' => { css: '.opacity\\:hover:hover { … }', layer: 'styles', breakpoint: null } }
console.log(cache);
```