export const partial = true;

```js
window.FS = window.FS || {};
FS.safelist = [
  'opacity:hover',
  {
    class: 'bg',
    states: [':hover', ':focus'],
    prefixes: ['md'],
    dark: true,
  },
];
```