export const partial = true;

```js
const safelist = FS.getSafelist();
// [{ baseClass: 'opacity', state: ':hover', fullClass: 'opacity:hover', isDark: false, prefix: null }]
console.log(safelist);
```